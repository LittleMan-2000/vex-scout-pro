import React from 'react'
import ReactDOM from 'react-dom/client'
import { VEXScoutPro } from './VEXScoutPro'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <VEXScoutPro />
  </React.StrictMode>,
)
